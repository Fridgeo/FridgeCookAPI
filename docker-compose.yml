services:
  db:
    image: postgres:alpine
    container_name: fridgecook_db_cook
    environment:
      POSTGRES_USER: prisma
      POSTGRES_PASSWORD: sauvage
      POSTGRES_DB: bdd-FridgeCook
    ports:
      - "54322:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: